- [FundCircle stack and routing](fundcircle-stack.md) — Vite+React+TS+Tailwind v4+Clerk+Firestore; path alias `@` maps to project root (not src/); shared layout components must live in root `components/` (not `src/components/`) to be importable as `@/components/...`.
- [AuthRedirectManager pattern](auth-redirect-pattern.md) — use PROTECTED_PREFIXES allowlist (dashboard/onboarding/profile etc.); all other paths are public by default. Never use a publicPaths denylist — it breaks as new public pages are added.
- [FundCircle auth patterns](fundcircle-auth.md) — Membership doc ID = `${organizationId}_${clerkUserId}`; roles stored as OWNER/AGENT/CUSTOMER (uppercase in Firestore); normalize with `normalizeClerkRole()`; ClerkProvider use `fallbackRedirectUrl` not deprecated `afterSignInUrl`.
- [Role resolution rules](role-resolution-rules.md) — Role ALWAYS resolved from org membership (Firestore doc first, Clerk org membership second); NEVER from publicMetadata/privateMetadata; manager role maps to organization_owner (same dashboard); `getRoleLabel()` in get-user-role.ts for display; `lib/validation.ts` is the single validation/sanitization source of truth.
- [Firestore manager role](firestore-manager-role.md) — isManager() helper in firestore.rules; isOrgMember() includes manager; isOwnerOrAgent() includes manager; isAdminOnly() = owner+manager (used for audit_logs read and org update); audit_logs readable by isAdminOnly not just isOwner.
- [Custom auth UI](fundcircle-custom-auth.md) — All Clerk UI components removed; custom dark-glassmorphism pages at `/auth/sign-in`, `/auth/sign-up`, `/auth/verify-email`, `/auth/forgot-password`, `/auth/reset-password`; use `useSignUp`/`useSignIn` hooks only; OTP stored in sessionStorage as `fc_signup_email` / `fc_reset_email`; all `/sign-in` redirects updated to `/auth/sign-in`.
- [FundCircle rollup fix](fundcircle-rollup.md) — Missing `@rollup/rollup-linux-x64-gnu` fixed by `npm install --include=optional` + `chmod +x node_modules/.bin/vite`; workflow must use port 5000 for webview outputType.
- [Firestore Replit connectivity](firestore-replit-connectivity.md) — WebChannel gRPC fails in Replit sandbox; fix: `experimentalForceLongPolling: true` + HMR guard in `lib/firebase.ts`; do NOT switch to `experimentalAutoDetectLongPolling` (causes Listen stream errors); hooks must call `setLoading(false)` on error or pages get stuck in skeleton state forever.
- [Onboarding infinite loop fix](onboarding-infinite-loop.md) — RoleRouter→/onboarding→RoleRouter loop; fix: check Clerk memberships before redirecting to /onboarding; guard OwnerOnboarding redirect with `if (success) return`; navOrgId via navigate state + sessionStorage.
- [Owner as default collector](owner-collector-rule.md) — Owner auto-becomes first collector on org creation; doc ID: `${orgId}_${userId}_default_collector`, role=AGENT, collector_type=OWNER, is_default=true; never block customer creation on agent count.
- [Clerk signIn complete+null sessionId bug](clerk-signin-complete-null-session.md) — `signIn.create()` can return `status:"complete"` with `createdSessionId:null` for invited users whose session was already activated; never gate navigation on `createdSessionId` truthy — check `status === "complete"` alone and navigate regardless.
- [One-owner-one-org architecture](org-ownership-rules.md) — Owners limited to 1 org (blocked in OrgCreate + OwnerOnboarding via Firestore OWNER role query); agents/customers multi-org via invite only; RoleRouter detects multi-org non-owners (role≠org:admin, no active org, 2+ memberships) → /org-select; OrgSelectorPage at src/pages/OrgSelectorPage.tsx; org switcher in AgentSidebar + CustomerDashboard header only (not in OrgDashboard/owner sidebar).
- [Org-scoped roles](org-scoped-roles.md) — Role cache key is `role_${userId}_${orgId}` (not just userId); OrgSelectorPage fetches Firestore organizationMembers doc after setActive to get per-org role; AuthCallback + RoleProtectedRoute guard against auto-activating first org for multi-org non-owners; users doc must NOT store a global role field.
- [No invitation flow](no-invitation-flow.md) — All invitation UI removed; only direct creation via createDirectMember() (server/index.ts /api/create-agent + /api/create-customer); PENDING_INVITED status badge maps to "Active" in UI; no "Invite" button labels anywhere; addMember() in services.ts is dead code (not called from UI).
- [FundCircle core modules](fundcircle-core-modules.md) — Full pigmy savings+loan system: receiptCounters Firestore collection for FC-{ORGSLUG}-{YYYYMMDD}-{SEQ4} daily sequence; savings_accounts.customerId = membershipDocId (not clerkUserId); loan_installments use `where("status","!=","PAID")` which needs composite Firestore index; CustomerDashboard uses useCollectionRealtimeRaw with explicit where("customerId","==",membershipId).
- [Customer portal architecture](customer-portal-arch.md) — CustomerDashboard.tsx is a lean shell (all Firestore listeners at top, passes data as props); 11 tab components in src/pages/customer/tabs/; notifications query uses only where("userId","==",clerkUserId) then filters by orgId in JS to avoid composite index; fc-input CSS utility class in src/index.css; useDocumentRealtime returns {data} not raw value — always destructure.
- [Loan collector assignment](loan-collector-assignment.md) — loans store loanAssignedCollectorId/Name/Role; AgentEMICollection scopes by loanAssignedCollectorId first, falls back to customer.assignedAgentId; approveLoan accepts optional collector params that override loan-creation values; OrgLoans auto-activates: createLoan→approveLoan in sequence; no PENDING UI for owner-created loans.
- [SearchSelect component](searchselect-component.md) — custom searchable dropdown at `components/ui/SearchSelect.tsx` (root, not src/); replaces all native `<select>` for collector/customer assignment; supports keyboard nav, sublabel, badge, clearable; shows search input only when options > 5.
- [Customer/loan form UX rules](customer-loan-form-ux.md) — phone inputs: type=tel, inputMode=numeric, digits-only onChange filter, maxLength=10; email inputs: auto-lowercase, inputMode=email; name inputs: allow only letters/spaces/periods; loan amount: inputMode=decimal with digit-only filter; nominee section is read-only card + Edit button (not always-open form).
- [Savings module removed](savings-module-removed.md) — Savings Management UI fully removed (OrgSavings, SavingsTab, PassbookTab deleted); CollectDialog now LOAN_EMI + GENERAL only; CustomerDashboard has no savings nav/listeners; savings_accounts Firestore collection still exists for legacy data but no UI reads it in owner/customer portals.
- [Security audit fixes](security-audit-fixes.md) — Fixed P0 security holes: authMiddleware on /api/create-agent + /api/create-customer; organizationMembers create restricted to isAdminOnly (not isOwnerOrAgent); customers read restricted to own-doc or isOwnerOrAgent; createDirectMember requires authToken param (Bearer header); offlineSync payments → collections (not transactions); firestore.indexes.json created for all composite queries.
- [Member creation architecture](member-creation-arch.md) — ALL Firestore writes for createDirectMember moved to server/index.ts via REST API (not client SDK); root cause was persistentLocalCache() buffering writes in IndexedDB without server sync; lib/firebase.ts now uses memoryLocalCache(); server handles: organizationMembers + customers + savings_accounts + users + audit_logs atomically with Clerk rollback on failure.
- [Agent customer visibility fix](agent-customer-visibility.md) — assignedAgentId must store Clerk user ID (user_xxx), NOT membership doc ID (orgId_userId); agent queries filter by user?.id (Clerk ID); collectorLabel count must look up by clerkUserId not c.id; role queries use equality not `in`; migrateCustomerAssignments() in lib/services.ts auto-fixes legacy docs; migration banner auto-appears in OrgCustomers when legacy data detected.
- [Enterprise validation pattern](enterprise-validation-pattern.md) — FieldError component uses `error` prop (NOT `message`); sanitizeName() takes 1 arg (no maxLength); validatePhone10/validateAmount/validateRate/validateName all accept optional options object as 2nd arg; sanitizeSearch() caps at 100 chars and strips SQL keywords; server helpers use srvSanitize/srvValidEmail/srvValidPhone/verifyIsOrgAdmin pattern.
- [Agent Panel V2 architecture](agent-panel-v2.md) — 5-tab (Today/Customers/Collections/History/Profile); shared CollectDialog at `components/agent/CollectDialog.tsx` (root, NOT src/components); only `toDate` exported from CollectDialog; no admin toggle, no customer creation, no search bars.
- [No customer type](no-customer-type.md) — customerType (SAVINGS/LOAN/SAVINGS_LOAN) fully removed; customers are plain members; CollectDialog detects active loan and auto-selects LOAN_EMI mode, falls back to GENERAL; OrgCustomers has single total stat card + no filter tabs + no Type column.
- [Owner collection permissions](owner-collection-permissions.md) — Owner collects via ₹ button on any customer row in OrgCustomers (not AgentDashboard); CollectDialog accepts optional collectedByRole+collectedById props; services default to "AGENT" when omitted; collections Firestore doc stores collectedByRole+collectedById; OrgCollections shows role badge (amber OWNER / sky AGENT) and collector filter includes owners who appear in data.
- [More page architecture](more-page-arch.md) — "More" is a full TabsContent tab (not a Sheet); MorePage.tsx has internal navigation via `page` state (list/profile/organization/notifications/support/about); cross-tab navigation via `fundcircle:switchTab` event; sub-page navigation from outside via `fundcircle:morePage` CustomEvent + sessionStorage key `fc_more_subpage`; header: 3-part (FundCircle brand | Org▼ chip | Avatar); org chip opens org actions sheet; avatar opens trimmed profile sheet; OrgSettings kept for desktop sidebar (cleaned: no UI Prefs, no Security, no Org ID, notifications are auto-save).
- [Clerk org reload causes fake page reload](clerk-org-reload-fix.md) — Server-side createOrganizationMembership triggers Clerk SSE → org briefly undefined → all useCollectionRealtime listeners tear down → UI looks like full reload. Fix: stable orgId ref in useCollectionRealtime + hasLoadedOnceRef in OrgDashboard.
- [Profile and logo photo management](photo-management.md) — Clerk is single source of truth for photos; after upload/remove sync imageUrl to organizationMembers + users Firestore docs; lists (OrgCustomers/OrgAgents/AgentCustomers) read imageUrl from organizationMembers; org logo remove requires server /api/remove-org-logo (Clerk Admin SDK deleteOrganizationLogo).
- [Loan Approval Module V2](loan-approval-v2.md) — 6 files: lib/loanEligibility.ts (async eligibility checks), LoanApprovalDialog.tsx (eligibility+checklist+3-action), LoanRejectDialog.tsx (reason picker), LoanRequestDocsDialog.tsx (doc selection+notifications), LoanRepaymentScheduleDialog.tsx (real-time schedule), LoanPortfolioAnalytics.tsx (stats+bar). OrgLoans.tsx: card-based UI, 5 tabs (pending/active/rejected/closed/all), ApplicationCard + LoanCard components with expand/collapse, optimistic status updates via optimisticStatuses map.
- [Loan lifecycle completions](loan-lifecycle-completions.md) — ReceiptModal has PDF download (jsPDF 90×230mm thermal); LoanClosureCertificate.tsx exports downloadClosureCertificate(); createLoan() accepts loanPurpose/nomineeName/nomineeRelation/nomineePhone; notifications sent on LOAN_APPROVED/REJECTED/EMI_COLLECTED/LOAN_CLOSED via createNotification(); OrgReports has 6 tabs (Overview/Loans/EMI Aging/Outstanding/Closed/Collector); OrgOverview has Closed Loans KPI card.
- [OrgReports tab architecture](orgreports-tabs.md) — 6 tabs: Overview (monthly bar chart + KPIs), Loans (all loans table + 4 KPI cards), EMI Aging (buckets + bar chart + table), Outstanding (active loans by balance), Closed (closed loans + est. repaid), Collector (per-collector EMI stats). All data from useCollectionRealtime; customer name resolved by members.find(x => x.id === loan.customerId).
